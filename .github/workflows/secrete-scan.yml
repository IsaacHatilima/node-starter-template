name: Scan Secrets

on:
  pull_request:
  push:
    branches: [ master ]

jobs:
  scan-secrets:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: List workspace
        run: ls -R .

      - name: Make script executable
        run: chmod +x scan-secrets.sh

      - name: Run secret scan
        run: ./scan-secrets.sh
